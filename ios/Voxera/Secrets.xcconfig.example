// Copy to Secrets.xcconfig (gitignored) and set the token (same as Android secrets.properties VOXERA_API_TOKEN).
// Required on the Mac before Archive → TestFlight (Scheme env vars do NOT apply to TestFlight).
VOXERA_API_TOKEN = paste_token_here
