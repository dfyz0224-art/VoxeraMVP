# Copy to keystore.properties (gitignored). Required for signed release / Play Console upload.
# storeFile — path relative to project root

storeFile=release.keystore
storePassword=your_store_password
keyAlias=voxera
keyPassword=your_key_password
