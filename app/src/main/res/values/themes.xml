<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Тема для splash screen - показывается до загрузки приложения -->
    <style name="Theme.Voxera.Splash" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="postSplashScreenTheme">@style/Theme.Voxera</item>
    </style>
    
    <!-- Основная тема приложения -->
    <style name="Theme.Voxera" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
