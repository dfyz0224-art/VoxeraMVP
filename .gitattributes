# Бинарники: при merge Git не вставляет conflict-маркеры в файл (лучше явно выбрать сторону).
*.png binary
*.jpg binary
*.jpeg binary
*.ogg binary
*.wav binary

# Shell scripts must be LF for Xcode PhaseScriptExecution on Mac.
*.sh text eol=lf
ios/scripts/* text eol=lf